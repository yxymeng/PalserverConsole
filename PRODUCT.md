# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

主要用户是在 Windows 主机上自行运行 PalServer 的个人服主或管理员。他们需要在服务器所在主机的桌面浏览器中进行日常管理，也需要在可信局域网内通过手机浏览器查看状态和完成适合移动端的管理操作。

## Product Purpose

PalServerConsole 是与 PalServer 运行在同一台 Windows 主机上的中文本地 Web 控制台。它把服务器状态、生命周期操作、运行监控、世界数据查询、配置草稿和低频维护集中到一个入口中。

产品成功意味着个人管理员无需在多个脚本、配置文件和命令行工具之间切换，就能看懂当前状态并完成操作；涉及真实服务器、配置和存档的写入仍有明确边界、前置条件和确认反馈。

## Positioning

产品不是通用远程运维面板，而是围绕 PalServer 本机管理闭环构建的专用控制台。它将 PalServer 的进程、存档、配置、备份与维护语义放在同一操作上下文中，并通过配置草稿、外部冲突检查、停服要求、操作倒计时和明确确认降低误操作风险。

## Operating Context

- 生产运行环境是 PalServer 所在的 Windows 主机；前端由本地后端提供，并通过浏览器访问。
- 主要场景包括桌面端日常管理，以及可信局域网内的手机访问。
- 本机访问默认免登录；局域网访问沿用游戏配置中的 `AdminPassword`。未配置时只能监听 `127.0.0.1`。
- 用户会围绕服务器运行状态、在线玩家、进程指标、世界快照、`PalWorldSettings.ini`、官方备份、SteamCMD 更新和审计记录进行操作。
- 真实服务器的恢复、配置应用、强制停止和更新属于维护窗口内的明确操作，不应由界面隐式触发。

## Capabilities and Constraints

- 一级信息架构固定为 `首页`、`世界数据`、`配置`、`维护`。
- `首页`负责服务器生命周期、运行状态和监控，并保持单一状态来源。
- `世界数据`提供玩家、帕鲁、公会和据点的只读浏览、搜索、关联筛选与详情查看；可读名称优先于内部 ID。
- `配置`区分控制台/实例设置与 `PalWorldSettings.ini`，保留“编辑 → 保存草稿 → 停服应用或重启并应用”的阶段语义。
- `维护`集中服务器更新、官方备份、审计与通知；危险操作必须单独确认。
- 普通 stop/restart 不得隐式应用配置草稿；配置冲突不得自动覆盖外部修改。
- 不把控制台设计成公网服务；LAN 模式只面向可信内网。
- 前端基于现有 React、TypeScript 和 Vite 工程继续建设；组件搭建使用 shadcn/ui 的组件与组合规范，并保留现有 `lucide-react` 图标体系。
- 桌面和手机浏览器都必须能够访问核心信息与操作流程；移动端不等同于原生 iOS 或 Android 应用。
- 验证使用 mock、合成数据、脱敏 fixture 或临时副本；不得把它们表述为真实服务器写入验证。

## Brand Commitments

- 产品名称为 `PalServerConsole`。
- 面向用户的界面与日常说明使用中文；技术错误名、API 名称、配置键和 HTTP 状态码保留英文原文。
- 产品语气应直接、克制、可执行，优先说明当前状态、影响和下一步。
- 不虚构服务器状态、玩家数据、验证结论、发布状态或安全能力。

## Evidence on Hand

- `README.md` 已记录产品能力、安全边界、Windows 运行方式和当前限制。
- `frontend/src` 中已有 React 页面、API contracts、单元测试与 Playwright E2E 测试。
- 现有功能覆盖服务器控制与监控、世界数据、配置草稿、备份、维护、审计和通知。
- `frontend/public/assets/pals/` 中已有按第三方许可管理的帕鲁资料与头像资源。
- 项目没有可用于宣传的客户案例、用户评价、公开运行指标或真实玩家数据；未来界面不得编造这些内容。

## Product Principles

1. 先让用户看清服务器当前状态，再提供与状态匹配的操作。
2. 高频查看保持直接紧凑，低频和危险操作保持分区、说明与明确确认。
3. 真实数据默认只读或先进入草稿；写入必须让目标、时机和后果可见。
4. 桌面端承载高密度操作，手机端保留核心信息与安全操作，不依赖横向宽屏才能完成流程。
5. 用户可读名称、中文状态和可恢复路径优先于内部 ID、技术缩写和隐式行为。

## Accessibility & Inclusion

- 核心流程需要支持键盘操作、明确焦点、可读状态文本和不依赖颜色单独传达含义。
- 桌面与手机浏览器都需要保持信息可读、触控目标可操作，并尊重 reduced-motion 偏好。
