# PalServerConsole Codex 工作规则

> 文档集版本：`2.1`（最终精简版）｜更新日期：`2026-08-07`

## 开始任务

1. 先运行 `git status --short`，保留用户已有改动。
2. 不默认通读全部 Markdown 或扫描全部源码。
3. 用户指定 `OPT-XX` 时，只在 `CODEX_OPTIMIZATION_ROADMAP.md` 中读取：
   - 对应状态表行；
   - 对应 `## OPT-XX` 模块段落；
   - 该段列出的入口文件及其直接依赖、相关测试。
4. 只有任务涉及产品边界、安全约束或两份文档冲突时，才读取精简后的 `plan.md`。
5. 阶段验收只读取路线文档中的状态表、对应“阶段验收”和相关已完成模块；不要重新规划功能。
6. `docs/archive/` 仅用于本地回滚；除非用户要求追溯历史，否则不要读取。
7. 修改前用中文简述本次范围、明确不做的内容和验证方式，然后直接实施。

定位模块可先使用：

```powershell
rg -n "^\| OPT-XX \||^## OPT-XX " CODEX_OPTIMIZATION_ROADMAP.md
```

## 开发边界

- 一个新对话只完成一个模块；依赖未标记为 `[x]` 时停止并说明。
- 先复现问题或补测试，再改实现；不适用时说明原因。
- 只改当前模块需要的文件，不顺手重构、升级大版本或格式化无关文件。
- 除非用户明确要求，不执行 commit、push、PR、Release。
- 不覆盖用户改动；发现额外问题只记录到路线文档的“待分流”，不扩展范围。

## 安全底线

- 不对真实 PalServer 自动执行恢复、删除、强制停止、配置覆盖或更新；真实写操作必须由用户明确批准维护窗口。
- 测试使用 mock、临时目录、合成数据或脱敏 fixture，不输出密码、token、真实存档或未脱敏玩家数据。
- 普通 stop/restart 不得隐式应用配置草稿；恢复、迁移和升级必须保留数据及失败恢复路径。
- 未设置 LAN 密码时只能监听 `127.0.0.1`，不得把控制台直接设计为公网服务。

## 按影响验证

- 后端：先运行相关 `pytest`；再按影响运行 `ruff`、`mypy` 和必要的全量 `pytest`。
- 前端：运行受影响测试；涉及前端交付时再运行 `lint`、`typecheck`、`test`、`build`。
- E2E：仅在页面交互、响应式布局或高风险确认流程变化时运行。
- 纯文档：检查链接、文件名、模块状态和 `git diff`，无需运行代码测试。
- 任何失败、跳过或环境缺口都保留关键英文原文，不得声称已全部通过。

## 完成与验收

- 完成模块后更新路线状态表，并在模块末尾追加简短完成记录：日期、修改文件、验证结果、未验证项和残余风险。
- 最终回复必须给出结论、改动文件、实际命令结果、未验证项、风险与回滚方式，然后停止，不开始下一模块。
- 用户要求“验收/审核”时只审不修，按 Critical/High/Medium/Low 报告；修复需另开模块或获得明确授权。
